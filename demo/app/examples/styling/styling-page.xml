<navigation:ExamplePage loaded="pageLoaded" xmlns:navigation="navigation/example-page"  xmlns="http://www.nativescript.org/tns.xsd" xmlns:picker="nativescript-picker">
	<GridLayout rows="50, *">
		<picker:PickerTextField hint="This is hint" padding="10"
								items="{{ pickerItems }}"
								textField="name"
								class="picker-field"
								pickerTitle="{{ pickerTitle }}">
			<picker:PickerTextField.itemTemplate>
				<GridLayout columns="auto, *" rows="auto, *" backgroundColor="lightBlue" marginBottom="40">
					<Label text="Statick text: " colSpan="2" class="item-template-top-label"></Label>
					<Label text="{{ name }}" col="0" row="1" class="item-template-label red-label" marginBottom="20"/>
					<Image src="{{ imageUrl }}" col="1" row="0" rowSpan="2" class="item-template-picture" />
				</GridLayout>
			</picker:PickerTextField.itemTemplate>
		</picker:PickerTextField>
	</GridLayout>
</navigation:ExamplePage>